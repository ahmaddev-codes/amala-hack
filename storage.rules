rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Restaurant photos - Public read, auth required for write
    match /restaurant-photos/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
